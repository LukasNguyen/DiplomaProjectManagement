@section Scripts{
    <script src="~/Assets/client/controllers/diplomaProjectRegistration/index.js"></script>
    <script>
        var DiplomaProjectRegistrationController = new DiplomaProjectRegistrationController();
        DiplomaProjectRegistrationController.initialize();
    </script>
}

<div class="page-title">
    <div class="title_left">
        <h3><small>Danh sách đề tài</small></h3>
    </div>
</div>

<div class="clearfix"></div>

<div class="row">
    <div class="col-md-12 col-sm-12 col-xs-12">
        <div class="x_panel">
            <div class="x_content">
                <select id="selectorRegistrationTime" class="pull-right">
                </select>
                <table class="table table-bordered">
                    <thead>
                        <tr>
                    </thead>
                    <tbody id="tbl-content"></tbody>
                </table>
            </div>
        </div>
    </div>
</div>
@*<script id="table-template" type="x-tmpl-mustache">
    <tr>
        <td id="diplomaProjectId">{{ID}}</td>
        <td>{{Name}}</td>
        <td>{{Description}}</td>
        <td scope="row">
            <a href="DiplomaProject/Edit/{{ID}}" class="btn btn-primary btn-sm"><i class="fa fa-pencil"></i></a>
            <button data-name="{{Name}}" data-id="{{ID}}" class="btn btn-danger btn-sm" id="btnDeleteDiplomaProject"><i class="fa fa-trash"></i></button>
        </td>
    </tr>
</script>*@